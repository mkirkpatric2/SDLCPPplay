Playing with CPP using SDL to build up a framework for basic game features with the intent to implement design patterns as I learn. 

DEPENDENCIES:   
SDL2  
SDL2_image

Dependencies are not included in source code. 